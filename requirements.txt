# Runtime dependencies for the Vercel Python function (api/index.py).
# (The full dev/test/UI stack lives in pyproject.toml; this is the lean subset
#  Vercel installs into the serverless function.)
fastapi>=0.111
pydantic>=2.7,<3
pydantic-settings>=2.3
python-dotenv>=1.0

langgraph>=0.2.40
langchain>=0.3.0
langchain-core>=0.3.0
langchain-anthropic>=0.2.0
langsmith>=0.1.130
tavily-python>=0.5.0

# Shared daily-cap counter for the public demo (serverless-safe).
upstash-redis>=1.1.0
